While updating README.md's #517/#519 paragraph (line 211) to add #524/#525/#526,
found the pre-existing "10 `_remember_forward_slash` call sites in total" claim
does not match reality even before this PR's changes.

Counted with `grep -o "_remember_forward_slash \"" scripts/doctor.sh
scripts/run-consolidation.sh scripts/session-start-hook.sh
scripts/lib-case-divergence.sh | wc -l` against `origin/main` (pre-#524/525/526):
12 real call sites, not 10. The gap is plausibly the two-calls-per-line storage-mode
`[[ == ]]` comparisons in doctor.sh (each of the two occurrences calls
`_remember_forward_slash` twice, once for REMEMBER_DIR and once for PROJECT_DIR) --
whoever wrote the "10" total may have counted "places" rather than raw call
occurrences, or just miscounted by hand.

Did not touch it -- deliberately left the pre-existing "10" number as-is and wrote
my own addition (#524/#525/#526) without restating a new absolute total, to avoid
compounding a wrong base number into a wrong new one. Left for /oss:curate or a
future PR to decide whether the paragraph's counting convention needs restating
site-by-site instead of as one number that has already drifted once.
