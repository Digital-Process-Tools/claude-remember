- #547: added three host badges to the README badge row -- Claude Code, Codex and Gemini CLI -- so a reader can see which agent hosts this plugin runs on without reading the tree for manifests. The Gemini CLI badge carries its own caveat paragraph beside the existing Windows one: its manifests are checked in and covered by `test_gemini_manifest_456`, `test_gemini_extension_533`, `test_gemini_project_dir_var_456` and `test_gemini_stdout_envelope_534`, but every one of those asserts manifest *shape* only -- no test, and no observation, has a hook firing under a running Gemini CLI, because headless OAuth returns `invalid_grant` in this environment (#532). Claude Code and Codex are observed hosts; Gemini CLI is a shipped-but-unverified one, and the README now says so rather than letting three identical-looking badges imply three identical-strength claims.
