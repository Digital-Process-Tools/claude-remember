- Fixed: `tests/fixtures/codex-rollout.jsonl`, committed as a real (sanitised) Codex capture, carried the maintainer's real home directory and OS username in two nested JSON-string blobs (lines 6-7) that a field-keyed sanitiser never walked -- `cwd`/`git`/`turn_context` were replaced, but a path embedded inside a serialised string value was not. Because `.agents/plugins/marketplace.json` declares a local install source (`"path": "./"`), the install copies `tests/` into every install, so this shipped into every 0.24.0 install rather than staying a repo-only artefact. Both blobs are re-sanitised to the fixture's existing `sanitized-user` placeholder convention, and `tests/test_no_real_home_paths_467.py` now fails the build if any `tests/fixtures/*` file names a `/Users/<x>/` or `/home/<x>/` segment other than that placeholder, so the next real capture cannot carry the next thing nobody thought to look at. (#467)
