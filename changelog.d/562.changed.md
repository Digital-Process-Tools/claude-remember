- Changed: reworded README.md and four docs/ pages so host-neutral prose says
  "coding agent" instead of "Claude Code" -- the badges list two hosts (Claude
  Code, Codex), and the pitch, trust-model and generic-behaviour sentences
  were still narrated as if there were one. Left "Claude Code" verbatim
  everywhere the sentence is actually Claude-Code-specific: install
  instructions, `.claude/` paths, the hook-name table, and the measured 60s
  hook-kill timeout in docs/configuration.md. Also folded "Claude Remember"
  to "Remember" after the first mention where a page had drifted from that
  convention. #562
