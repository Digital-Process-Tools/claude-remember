- Fixed: five comments and one test docstring still described the merged config file as created `0600 per PID`, a claim #429 stopped being true when it replaced the PID-suffixed `remember-config-$$.json` literal path with an unpredictable `mktemp` name to close a symlink-planting TOCTOU. A reader debugging a stale merged config would grep for a filename that is never generated. Two more sites, in `tests/test_session_end_hook_345.py`, named the old literal path directly in a test comment and were just as stale but were not in #429's own count. All eight now describe the current mechanism: `0600`, an unpredictable `mktemp` name, one per invocation, under an EXIT trap. `tests/test_layered_config.py` gained a new assertion, `test_merged_config_is_0600_and_not_pid_named`, pinning both the mode and the mktemp-shaped basename so a regression to either the old literal path or a permissive mode fails a test rather than only reading stale in a comment. (#437)
