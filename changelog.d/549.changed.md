- #549: the README shrank from 77KB to about 21KB. Eight sections that recorded how a defect was found rather than what a reader needs before installing moved verbatim, not rewritten, into their own `docs/` pages: one install page per host (Claude Code with the official-marketplace lag, manual install and version check; Codex; Gemini CLI), Windows, hooks and the `hooks.d/` listener contract, diagnostics, handoff delivery, and how memory files are written. Each one is listed under `## Reference`, and every section that moved leaves a short summary and a link in its place. Same convention as #505.
