- Fixed: the `#346` positive-control assertion in
  `test_session_start_names_an_oversized_memory_file_instead_of_injecting_it`
  elided the hook's full stdout on failure -- a flake seen once, where a
  healthy `now.md` was not injected while the rest of the session-start
  payload was, printed only pytest's own truncated string diff and gave the
  next occurrence nothing to work from. All four assertions in that test now
  dump the full payload (a few kilobytes) in their failure message, so the
  next sighting carries its own evidence instead of needing a reproduction
  that has not happened yet (#707).
