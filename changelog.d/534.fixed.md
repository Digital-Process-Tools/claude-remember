- #534: the shell layer's own `CLAUDE_PROJECT_DIR`-unset comments and README's Codex/Gemini prose both still asserted "Codex and Gemini CLI never set `CLAUDE_PROJECT_DIR`" as shared fact, even though #456 had already found that wrong for Gemini CLI specifically (its own bundled docs list it as a compatibility alias Gemini sets). `scripts/resolve-paths.sh`'s `REMEMBER_HOOK_CWD` fallback and `scripts/lib-env-cache.sh`'s cache-key fallback needed only corrected comments -- both stay correct as fallbacks for any host that genuinely leaves the variable unset (Codex, live-confirmed via `tests/fixtures/codex-env-463.txt`), and Gemini setting the variable just means priority 1 wins and the fallback is never reached on that host. `scripts/user-prompt-hook.sh`'s `UserPromptSubmit` JSON-envelope branch needed an actual behavioural fix, not just prose: it used to key on "is `CLAUDE_PROJECT_DIR` set" as a stand-in for "is this Codex", which would have silently routed a Gemini session through Codex's JSON envelope -- unverified, since Gemini CLI's own `BeforeAgent` stdout contract has never been observed and #532 blocks a live session. It now keys on Codex's own live-verified signature (`CODEX_SESSION_ID`/`CODEX_THREAD_ID`, the same pair `pipeline/host.py`'s `CODEX.signature_vars` already uses) instead, so the JSON envelope is Codex-only by construction and every other host, Gemini included, gets plain stdout -- a documented, defensible default, not a claim about what Gemini's own contract wants. TDD via `tests/test_gemini_stdout_envelope_534.py`, red on the pre-fix gate (a host with neither `CLAUDE_PROJECT_DIR` nor a Codex signature used to get the JSON envelope by default); `tests/test_codex_upsubmit_stdout_451.py`'s own Codex fixture updated to set the same signature, matching a real `codex exec` process. README corrected in every place it repeated the old shared-fact claim.
